/**
 * Публичный URL логотипа для веб-UI (`public/Snipcast logo.svg`).
 * Системные иконки (.ico/.icns/PNG в `src-tauri/icons`) — `npm run icons`
 * (генерация из `public/Snipcast-icon-square.svg` и очистка мобильных артефактов).
 */
export const SNIPCAST_LOGO_SRC = "/Snipcast%20logo.svg";
